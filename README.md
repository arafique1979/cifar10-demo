# CIFAR-10 Demo

Simple PyTorch project for training a small CNN on the CIFAR-10 dataset.

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python train.py


